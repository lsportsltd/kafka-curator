trigger:
  branches:
    include:
     - main

resources:
  repositories:
    - repository: pipeline-templates
      type: git
      name: Devops/pipeline-templates
      ref: feature/ansible_env

stages:
- template: jobs/kafka-curator.yaml@pipeline-templates